apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pydis-wtf
  namespace: nginx-gateway
spec:
  secretName: pydis.wtf-tls
  dnsNames:
    - pydis.wtf
    - "*.pydis.wtf"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
