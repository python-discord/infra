apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pydis-com
  namespace: nginx-gateway
spec:
  secretName: pydis-com-tls
  dnsNames:
    - pydis.com
    - "*.pydis.com"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
