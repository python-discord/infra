apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: owlcorp-uk
  namespace: nginx-gateway
spec:
  secretName: owlcorp.uk-tls
  dnsNames:
    - owlcorp.uk
    - "*.owlcorp.uk"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
